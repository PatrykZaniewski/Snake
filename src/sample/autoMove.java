package sample;

import javafx.scene.input.KeyCode;

public class autoMove extends Thread {

    private KeyCode key;
    private addNew add;


    @Override
    public void run() {
        while(true)
        {
            try
            {
               // moveImage move = new moveImage(lista, playgroundC, add);
            }
            catch (Exception e)
            {

            }
        }
    }

    public addNew returnAdd()
    {
        return null;
    }
}
